---
title: "闪卡 - 面试高频"
created: 2026-05-19
updated: 2026-05-19
tags:
  - 类型/闪卡
  - 分类/面试
status: complete
category: interview
---

 #flashcards/面试高频

## DDD 中聚合根的核心职责是什么？
?
- **核心职责**：维护业务不变量（Invariants）和确保事务一致性边界
- **关键点**：外部只能通过 ID 引用聚合根，严禁直接访问聚合内部成员
- **跨聚合一致性**：聚合内强一致，跨聚合最终一致
- **实现方式**：通过领域事件配合 MQ 或 Saga 模式
> 来源：[[简历涉及知识点针对性梳理]]

---

## 什么是绞杀者模式（Strangler Fig）？适用什么场景？
?
一种渐进式重构策略，用于将老旧系统逐步迁移到新架构：
1. 建立防腐层（ACL）隔离新旧模型
2. 场景测试驱动：通过静态分析识别逻辑分支
3. 读写分离灰度：
   - 读：新旧对比
   - 写：旧逻辑主导 → 影子运行对比 → 业务开关切换 → 下线旧逻辑
> 来源：[[简历涉及知识点针对性梳理]]

---

## 如何保证分布式事务的最终一致性？
?
1. **Transactional Outbox（事务发件箱）**：在同一个本地事务中写业务数据和消息记录
2. **FlinkCDC/Canal 监听**：监听发件箱表的 Binlog 变更
3. **MQ 投递**：将消息投递到消息队列
4. **消费端幂等**：消费端保证幂等性，实现跨服务最终一致性
> 来源：[[简历涉及知识点针对性梳理]]

---

## HashMap 的底层实现原理？
?
JDK 8：数组 + 链表 + 红黑树
1. 初始容量 16，负载因子 0.75
2. `hash(key) & (n-1)` 计算桶位置
3. 链表长度 >= 8 且数组长度 >= 64 → 转红黑树
4. 红黑树节点 <= 6 → 退化为链表
5. 扩容：2 倍扩容，rehash
> 来源：[[HashMap与ConcurrentHashMap]]

---

## ConcurrentHashMap JDK 7 和 JDK 8 的实现区别？
?
- **JDK 7**：Segment 分段锁，每个 Segment 是一个小的 HashMap
- **JDK 8**：取消 Segment，用 CAS + synchronized（锁桶节点）
  - 读操作无锁（volatile 保证可见性）
  - 写操作先尝试 CAS，失败再用 synchronized
  - 并发度 = 桶的数量，比 JDK 7 大幅提升
> 来源：[[HashMap与ConcurrentHashMap]]

---

## 如何排查线上 CPU 100% 的问题？
?
1. `top` 确定占用 CPU 最高的进程 PID
2. `top -Hp PID` 找到占用最高的线程 TID
3. `printf "%x\n" TID` 将 TID 转为十六进制
4. `jstack PID | grep 十六进制TID -A 30` 查看线程堆栈
5. 定位到具体代码行
> 来源：[[面试问题记录]]

---

## 什么是 Sentinel 的自适应限流？
?
根据系统的 CPU 使用率、Load、RT（响应时间）等指标动态调整限流阈值，在系统接近崩溃边缘时自动限流，实现"自动驾驶"式的系统保护。
> 来源：[[面试问题记录]]

---

## 在重构万行级存储过程时，如何保证逻辑迁移的正确性？
?
1. **静态分析**：SQLFlow/手工梳理逻辑分支
2. **影子运行（Shadowing）**：Java 层实现相同逻辑，异步对比结果
3. **绞杀者模式**：按业务模块逐个剥离，通过 ACL 与旧库通信
4. **场景测试**：基于生产采样数据构建全路径回归测试
> 来源：[[面试问题记录]]

---

## 双向消息确认为什么不是解决分布式事务的最佳实践？
?
1. **无限递归**：确认消息本身也可能丢失，导致状态悬挂
2. **复杂度高**：增加网络开销和系统耦合
3. **更好的方案**：Outbox Pattern（本地事务表）+ 幂等消费，或 Saga 模式
> 来源：[[面试问题记录]]

---

## 如何避免 DDD 落地时写成"伪充血模型"？
?
1. 识别聚合根：业务操作只能通过聚合根进行
2. 区分 Domain Service 与 Entity：跨实体协调放 Service，单实体状态变更放 Entity
3. 隔离副作用：Repository 接口定义在领域层，实现在基础设施层
> 来源：[[面试问题记录]]

---

## 内存分配热点图（Flame Graph）相比 Heap Dump 的优势？
?
- Flame Graph 展示"哪里在产生对象"
- Heap Dump 展示"哪里留存了对象"
- 对于频繁 Minor GC 的性能瓶颈，火焰图更有效
> 来源：[[面试问题记录]]
