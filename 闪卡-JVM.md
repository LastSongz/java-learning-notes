---
title: "闪卡 - JVM"
created: 2026-05-19
updated: 2026-05-19
tags:
  - 类型/闪卡
  - 分类/jvm
status: complete
category: jvm
---

 #flashcards/JVM

## JVM 运行时数据区中，哪些是线程私有的？哪些是线程共享的？
?
线程私有：程序计数器、虚拟机栈、本地方法栈
线程共享：堆、元空间（方法区的实现）
> 来源：[[JVM内存结构]]

---

## 程序计数器有什么特殊之处？
?
- 唯一不会产生 OutOfMemoryError 的区域
- 执行 Native 方法时计数器为空（Undefined）
- 作用：字节码行号指示器，线程恢复执行时从此位置继续
> 来源：[[JVM内存结构]]

---

## 虚拟机栈的栈帧包含哪几部分？
?
1. **局部变量表**：存放方法参数和局部变量，容量编译时确定
2. **操作数栈**：方法执行时的临时操作空间
3. **动态链接**：解析符号引用为直接引用
4. **方法返回地址**：方法退出后需要恢复的执行位置
> 来源：[[JVM内存结构]]

---

## StackOverflowError 和 OutOfMemoryError 分别在什么场景下发生？
?
- **StackOverflowError**：栈深度超过限制（如递归无退出条件）
- **OutOfMemoryError**：无法申请足够内存（如创建大量线程）
- 控制参数：`-Xss`，默认 1MB
> 来源：[[JVM内存结构]]

---

## 为什么 JDK8 用元空间替代了永久代？
?
1. **位置**：永久代在堆内受堆大小限制，元空间使用本地内存（堆外）
2. **大小**：永久代固定难调整，元空间动态增长
3. **Full GC**：永久代需要 Full GC 才回收，元空间更灵活
4. **调优**：用 `MaxMetaspaceSize` 替代 `PermGenSize`
> 来源：[[JVM内存结构]]

---

## 堆的分代结构是怎样的？默认比例是多少？
?
分代结构：
- 年轻代：Eden + Survivor 0 + Survivor 1
- 老年代

默认比例：
- `NewRatio=2`：老年代/年轻代 = 2:1
- `SurvivorRatio=8`：Eden/Survivor = 8:1
> 来源：[[JVM内存结构]]

---

## 对象在堆中的分配流程是什么？
?
1. 检查 TLAB（线程本地分配缓冲）→ 成功则完成
2. 在 Eden 区分配 → 成功则完成
3. Eden 满了触发 Minor GC → 再次尝试 Eden
4. 存活对象进入 Survivor，年龄+1
5. 年龄达到阈值或 Survivor 满了 → 晋升老年代
6. 老年代也满了 → 触发 Full GC
7. Full GC 后仍失败 → OOM
> 来源：[[JVM内存结构]]

---

## JMM 的三大特性是什么？
?
1. **可见性**：一个线程对共享变量的修改，其他线程能立即看到（volatile）
2. **原子性**：一个操作要么全部执行，要么全部不执行（synchronized/Atomic）
3. **有序性**：程序执行顺序按代码顺序，防止指令重排（volatile/happens-before）
> 来源：[[JVM内存结构]]

---

## happens-before 的核心规则有哪些？
?
1. 程序顺序规则：同线程中前面的操作 happens-before 后面的操作
2. volatile 写规则：volatile 写 happens-before 后续对该变量的读
3. 解锁规则：unlock happens-before 后续对同一锁的 lock
4. 线程 start 规则：start() happens-before 该线程的任何操作
5. 传递性：A→B 且 B→C，则 A→C
> 来源：[[JVM内存结构]]

---

## volatile 底层是如何实现的？
?
通过内存屏障（Memory Barrier）实现：
- **写操作**：插入 StoreStore + StoreLoad 屏障，确保写入刷新到主内存
- **读操作**：插入 LoadLoad + LoadStore 屏障，确保读到最新值
- StoreLoad 是最重的屏障，跨越它后面的所有读写操作
> 来源：[[JVM内存结构]]

---

## 对象的内存布局包含哪几部分？
?
1. **对象头**：Mark Word（哈希码、锁信息、GC分代年龄）+ 类型指针
2. **实例数据**：父类字段 + 子类字段（相同宽度字段分配在一起）
3. **对齐填充**：对象大小必须是 8 字节整数倍，不足补齐
> 来源：[[JVM内存结构]]

---

## 可达性分析中的 GC Roots 有哪些？
?
1. 虚拟机栈中本地变量表引用的对象
2. 方法区中静态属性引用的对象
3. 方法区中常量引用的对象
4. 本地方法栈中 JNI 引用的对象
5. 所有被 synchronized 持有的对象
6. 内部引用（ClassLoader、异常对象等）
> 来源：[[JVM内存结构]]

---

## 三种垃圾收集算法的优缺点？
?
1. **标记-清除**：简单但有内存碎片
2. **标记-复制**：无碎片但可用内存减半（适合年轻代）
3. **标记-整理**：无碎片且利用率高但整理有性能开销（适合老年代）
4. **分代收集**：年轻代用复制算法，老年代用整理算法
> 来源：[[JVM内存结构]]

---

## CMS 收集器的四个阶段是什么？
?
1. **初始标记**：标记 GC Roots 直接关联的对象（STW）
2. **并发标记**：遍历对象图，耗时最长（不停顿）
3. **重新标记**：修正并发期间变动的对象（STW）
4. **并发清除**：清理死亡对象（不停顿）

缺点：对 CPU 敏感、无法处理浮动垃圾、产生内存碎片
> 来源：[[JVM内存结构]]

---

## G1 收集器相比 CMS 有什么优势？
?
1. **Region 分区**：将堆划分为多个 Region，避免内存碎片
2. **可预测停顿**：通过 `-XX:MaxGCPauseMillis` 控制停顿时间
3. **优先回收**：优先回收价值最大（垃圾最多）的 Region
4. **Mixed GC**：可同时回收年轻代和老年代
5. JDK 9+ 默认收集器，适合大内存场景
> 来源：[[JVM内存结构]]

---

## Full GC 频繁的常见原因有哪些？
?
1. 老年代空间不足
2. 元空间不足
3. 分配担保失败
4. CMS GC 失败（Concurrent Mode Failure）
5. 老年代碎片化严重
> 来源：[[JVM内存结构]]

---

## 如何排查线上频繁 Full GC？
?
1. `jstat -gcutil <pid> 1000` 监控 GC 频率和耗时
2. `jmap -dump:format=b,file=heap.hprof <pid>` 导出堆转储
3. 使用 MAT 分析 Dominator Tree 找最大对象
4. 通过 Path to GC Roots 查找引用链
5. 定位到具体代码行修复
> 来源：[[JVM内存结构]]
