---
title: "闪卡 - 数据库"
created: 2026-05-19
updated: 2026-05-19
tags:
  - 类型/闪卡
  - 分类/数据库
status: complete
category: database
---

 #flashcards/数据库

## MySQL InnoDB 和 MyISAM 的区别？
?
| 对比项 | InnoDB | MyISAM |
|--------|--------|--------|
| 事务 | 支持 | 不支持 |
| 行锁 | 支持 | 只有表锁 |
| 外键 | 支持 | 不支持 |
| MVCC | 支持 | 不支持 |
| 崩溃恢复 | 支持（redo log） | 不支持 |
| 全文索引 | 5.6+ 支持 | 支持 |
| 适用场景 | 高并发 OLTP | 读多写少、不需事务 |
> 来源：[[MYSQL面试突击--存储引擎]]

---

## B+ 树索引相比 B 树有什么优势？
?
1. **非叶子节点不存数据**：每个节点能存更多键值，树更矮，IO 更少
2. **叶子节点形成链表**：范围查询效率高，只需遍历链表
3. **查询性能稳定**：每次都要走到叶子节点，路径长度一致
4. **适合磁盘存储**：一个节点恰好对应一个磁盘页
> 来源：[[MYSQL面试突击---索引]]

---

## 什么是聚簇索引和非聚簇索引？
?
- **聚簇索引**：叶子节点存储完整的行数据，一张表只能有一个（通常是主键）
- **非聚簇索引（二级索引）**：叶子节点存储主键值，查询非索引列需要回表
- **覆盖索引**：查询的列都在索引中，不需要回表，效率最高
> 来源：[[MYSQL面试突击---索引]]

---

## 索引什么时候会失效？
?
1. 对索引列使用函数或计算
2. 隐式类型转换（如 varchar 列用 int 查询）
3. 使用 `!=` 或 `NOT IN`
4. 使用 `LIKE '%abc'`（左模糊）
5. 联合索引不满足最左前缀原则
6. OR 条件中有未建索引的列
7. 数据量太小，优化器认为全表扫描更快
> 来源：[[MYSQL面试突击---索引]]

---

## 联合索引的最左前缀原则是什么？
?
联合索引 `(a, b, c)` 的匹配规则：
- `WHERE a=?` → 命中索引
- `WHERE a=? AND b=?` → 命中索引
- `WHERE a=? AND b=? AND c=?` → 完全命中
- `WHERE b=?` → 不命中（跳过了 a）
- `WHERE a=? AND c=?` → 只命中 a 的部分

遇到范围查询（>、<、BETWEEN）会停止匹配后续列
> 来源：[[MYSQL面试突击---索引]]

---

## MVCC 的实现原理是什么？
?
MVCC 通过三个组件实现：
1. **隐藏字段**：每行有 DB_TRX_ID（最后修改的事务ID）、DB_ROLL_PTR（回滚指针）
2. **Undo Log 版本链**：通过回滚指针串起该行的所有历史版本
3. **Read View**：记录当前活跃事务列表，用于判断版本可见性

- RC 隔离级别：每次 SELECT 生成新的 Read View
- RR 隔离级别：只在第一次 SELECT 生成 Read View
> 来源：[[MVCC多版本并发控制]]

---

## MySQL 事务的四大特性（ACID）？
?
- **原子性（Atomicity）**：事务要么全部成功，要么全部回滚（undo log 保证）
- **一致性（Consistency）**：事务前后数据满足完整性约束
- **隔离性（Isolation）**：并发事务之间互不干扰（锁 + MVCC 保证）
- **持久性（Durability）**：事务提交后数据永久保存（redo log 保证）
> 来源：[[MYSQL面试突击---事务与锁]]

---

## MySQL 的四种隔离级别？
?
| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
|----------|------|------------|------|
| READ UNCOMMITTED | 有 | 有 | 有 |
| READ COMMITTED (RC) | 无 | 有 | 有 |
| REPEATABLE READ (RR) | 无 | 无 | 部分有 |
| SERIALIZABLE | 无 | 无 | 无 |

MySQL 默认 RR，通过 MVCC + 间隙锁在很大程度上避免幻读
> 来源：[[MYSQL面试突击---事务与锁]]

---

## InnoDB 有哪几种锁？
?
1. **行锁（Record Lock）**：锁定索引记录
2. **间隙锁（Gap Lock）**：锁定索引记录之间的间隙，防止插入
3. **临键锁（Next-Key Lock）**：行锁 + 间隙锁，左开右闭区间
4. **插入意向锁**：INSERT 操作时加的间隙锁，相互不阻塞
5. **意向锁（IS/IX）**：表级锁，表示事务打算加行锁

RR 级别默认使用 Next-Key Lock，RC 级别只有 Record Lock
> 来源：[[MYSQL面试突击---事务与锁]]

---

## 分库分表有哪些方案？各自的适用场景？
?
**垂直分库**：按业务拆分（如订单库、用户库）
**垂直分表**：将大字段拆到扩展表（如商品详情单独表）
**水平分库**：按分片键将数据分散到不同数据库实例
**水平分表**：按分片键将数据分散到同库的不同表

分片策略：
- Range（范围）：按时间、ID 范围
- Hash（哈希）：按某字段取模
- 一致性 Hash：减少数据迁移

中间件：ShardingSphere、MyCat
> 来源：[[MYSQL面试突击---分库分表]]

---

## MySQL 主从复制的原理？
?
1. **Master**：将变更写入 Binlog
2. **Slave IO Thread**：连接 Master 请求 Binlog，写入 Relay Log
3. **Slave SQL Thread**：读取 Relay Log，重放 SQL 语句

同步模式：
- 异步复制：Master 不等 Slave 确认（默认）
- 半同步复制：至少一个 Slave 确认后才返回
- 全同步：所有 Slave 确认（性能差，少用）
> 来源：[[mysql主从复制原理]]
