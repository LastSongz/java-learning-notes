---
title: "PostgreSQL面试速记"
created: 2026-06-24
updated: 2026-07-05
tags:
  - 分类/面试
  - 主题/PostgreSQL
  - 类型/索引
status: seed
category: database
---

# PostgreSQL 面试速记

> 这页用于整理 PostgreSQL 在面试里最容易被追问的表设计、约束与索引表达，尤其适合和 Rails / 资源建模场景一起讲。

## 当前最容易被问的点

### 主键为什么不是默认 UUID
- PostgreSQL 支持 UUID，但不是默认就用 UUID。
- Rails + PostgreSQL 常见默认主键仍是整数型，通常是 `bigint` / identity。
- 如果业务没有分布式生成主键、跨系统合并等强诉求，`bigint` 是更自然的默认选择。

### comment / migration
- PostgreSQL 支持 `COMMENT ON TABLE/COLUMN`，适合把表意图写清楚。
- 在 Rails 语境下，可以把它理解为 migration 里可维护的表结构元信息，而不只是“建完表再手工备注”。

### partial index
- `delete_flag` 这种低基数字段不适合盲目建普通单列索引。
- PostgreSQL 提供 partial index，适合“只给活跃数据建立索引”这类场景。
- 面试表达重点是“根据查询模式决定索引”，不是为了炫技而上特性。

### 唯一约束与并发防重
- 一人一次操作这类规则，最终仍要落到数据库唯一约束兜底。
- 应用层校验只负责更友好的返回，不能替代数据库约束。

### 外键是不是一定要建
- 建不建数据库外键是工程取舍，不是会不会建的问题。
- 如果团队更重视迁移灵活性、批量导入和数据修复便利，可以把完整性更多放到应用层校验、唯一索引和事务控制里。
- 面试里关键是把取舍讲清楚，而不是把某一种做法说成绝对标准答案。

## 常见建模追问场景

1. 用户、投票、审批这类“一人一次”的动作，要能说明唯一索引如何兜底。
2. 图片、附件、明细项这类一对多关系，为什么要单独建子表，而不是平铺字段。
3. `position` 这类展示顺序字段，为什么不能简单依赖 id 或插入顺序。
4. 读多写少的计数场景，为什么可能需要 `counter_cache` / 冗余计数字段，而不是每次实时 `count(*)`。
5. 审计字段、软删除和低基数字段，如何结合查询模式设计索引。

## 和 MySQL 的表达差异

- 不要说 PostgreSQL “比 MySQL 高级很多”，这种说法没价值。
- 更稳妥的表达是：二者都能满足常规 OLTP，PostgreSQL 在类型系统、扩展能力、部分特性表达上更灵活。
- 如果你没有长期 PostgreSQL 实战，承认这个边界，比硬讲内部实现更可靠。

## 关联

- [[Rails RESTful Routing 面试速记]]
- [[架构能力与工程实践面试清单]]
- [[MOC-Java面试]]