---
title: "部分题目记录"
created: 2026-05-19
updated: 2026-05-19
tags:
  - 分类/面试
  - 分类/架构
status: complete
category: interview
---

### 📌 选题列表（第一轮）

- **【1. DDD 实战】** 在供应链的履约（Fulfillment）与库存（Inventory）上下文中，如果发生极端高并发下的超卖，你是如何通过限界上下文的交互和最终一致性来处理的？别给我背概念，说具体的聚合根设计、领域事件和补偿机制。
    
- **【2. 复杂重构】** 你简历里写了万行存储过程重构。把包含复杂核心业务逻辑的 Oracle 存储过程迁移到 Java 微服务，且要求业务**零停机**。你具体怎么落地的？如果你敢提简单的“停机发版、新老库双写”，这场面试现在就可以结束了。
    
- **【3. [[JVM内存结构]]/性能优化】** 线上订单系统频繁触发 Full GC，且伴随着 CPU 飙高，业务场景是高并发的库存预占。给我一个排查和解决的 SOP（标准作业程序）。不要只背 `jstack`、`jmap` 等基础命令，告诉我你如何结合具体的内存泄漏现象定位到业务代码的那一行。
    
- **【4. 工程卓越(XP/TDD)】** 面对一个毫无单元测试、满篇都是直接 `new` 数据库连接和几百行静态方法调用的遗留单体“屎山”，老板现在要求你加一个“保税仓优先扣减”的新库存策略，且要求必须用 **TDD** 的方式开发。你会如何解耦、打桩并构建测试安全网？
    
- **【5. 架构选型】** 针对千万级并发的秒杀系统，你如何设计多级限流？别光提个 Sentinel 凑数，网关层的粗粒度控制和核心业务层的精准准入控制具体怎么拆分和协同？如何保证限流不仅拦得住，还要放得准？
    
- **【6. Java 核心底层】** 既然你深耕后端，聊点底层的。JDK 8 中 `Concurrent[[HashMap与ConcurrentHashMap]]` 放弃了分段锁，而 `LongAdder` 引入了 Cell 数组机制。结合硬件级别的 MESI 缓存一致性协议和 CAS，给我讲透在高并发场景下，这些底层组件是如何极致榨干硬件性能以避免伪共享（False Sharing）的？
    
- **【7. 综合应用/系统设计】** 微服务架构下的分布式事务。供应链场景中常见的：订单创建 -> 库存扣减 -> 冻结资金。不用 2PC，如果采用 Saga 模式，你的正�