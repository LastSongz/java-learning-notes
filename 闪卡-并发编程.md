---
title: "闪卡 - 并发编程"
created: 2026-05-19
updated: 2026-05-19
tags:
  - 类型/闪卡
  - 分类/java
  - 主题/锁与并发
status: complete
category: concurrency
---

 #flashcards/并发编程

## synchronized 的锁升级过程是怎样的？
?
无锁 → 偏向锁 → 轻量级锁 → 重量级锁
1. **无锁**：对象刚创建，偏向标志=0，锁标志=01
2. **偏向锁**：第一个线程访问时，将线程 ID 存入 Mark Word
3. **轻量级锁**：有竞争时，指向栈中锁记录（CAS 自旋）
4. **重量级锁**：竞争加剧，指向互斥量（OS 层面互斥）
> 来源：[[锁机制与并发原语]]

---

## CAS 是什么？有什么问题？
?
CAS（Compare And Swap）：比较并交换，乐观锁的实现方式。
- 原理：读取值 → 比较 → 相同则更新，否则重试
- 问题：
  1. **ABA 问题**：值被改为 B 又改回 A，CAS 误认为没变。解决方案：AtomicStampedReference 加版本号
  2. **自旋消耗 CPU**：竞争激烈时 CAS 不断失败重试
  3. **只能保证单个变量的原子操作**
> 来源：[[锁机制与并发原语]]

---

## AQS 的核心原理是什么？
?
AQS（AbstractQueuedSynchronizer）是 Java 并发的核心框架：
1. 用 `volatile int state` 表示锁状态
2. 用 **CLH 双向队列** 管理等待线程
3. 获取锁失败时线程加入队列尾部并自旋/阻塞
4. 释放锁时唤醒后继节点
5. 为什么用双向队列：取消竞争、唤醒机制、公平性判断都需要向前遍历
> 来源：[[锁机制与并发原语]]

---

## volatile 能保证线程安全吗？
?
不能完全保证。volatile 只保证：
1. **可见性**：修改后立即刷新到主内存
2. **有序性**：防止指令重排
3. **不保证原子性**：`count++` 这种复合操作仍然不安全

场景选择：
- 状态标志位（boolean flag）→ volatile 足够
- 计数器（count++）→ 需要用 AtomicInteger
> 来源：[[锁机制与并发原语]]

---

## ReentrantLock 和 synchronized 的区别？
?
| 对比项 | synchronized | ReentrantLock |
|--------|-------------|---------------|
| 实现 | JVM 层面 | API 层面（AQS） |
| 锁释放 | 自动 | 手动 unlock（必须在 finally 中） |
| 可中断 | 不可 | lockInterruptibly() 可中断 |
| 公平锁 | 只有非公平 | 可选公平/非公平 |
| 条件变量 | 只有一个 wait/notify | 多个 Condition |
| 锁绑定 | 不可绑定 | 可绑定多个 Condition |
> 来源：[[锁机制与并发原语]]

---

## LongAdder 为什么比 AtomicLong 性能好？
?
AtomicLong 在高并发下所有线程竞争同一个变量的 CAS，大量自旋失败。
LongAdder 使用 Cell 数组（分段思想）：
1. 不同线程操作不同的 Cell，减少竞争
2. Cell 通过 `@Contended` 注解避免伪共享
3. 最终通过 `sum()` 汇总所有 Cell 的值
4. sum() 不保证强一致性（不加全局锁）
> 来源：[[锁机制与并发原语]]

---

## 什么是伪共享？如何避免？
?
伪共享：多个线程同时修改同一 CPU 缓存行（64字节）上的不同变量，导致缓存行频繁失效。
避免方案：
1. `@Contended` 注解（JDK8+）
2. 字节填充（手动 padding）
3. LongAdder 的 Cell 数组使用 `@Contended` 避免伪共享
> 来源：[[锁机制与并发原语]]

---

## 线程池的 7 个核心参数是什么？
?
1. **corePoolSize**：核心线程数
2. **maximumPoolSize**：最大线程数
3. **keepAliveTime**：非核心线程空闲存活时间
4. **unit**：存活时间单位
5. **workQueue**：任务队列
6. **threadFactory**：线程工厂
7. **handler**：拒绝策略
> 来源：[[锁机制与并发原语]]

---

## 线程池的 4 种拒绝策略？
?
1. **AbortPolicy**：抛出 RejectedExecutionException（默认）
2. **CallerRunsPolicy**：由调用线程执行任务
3. **DiscardPolicy**：直接丢弃，不抛异常
4. **DiscardOldestPolicy**：丢弃队列最老的任务，重新提交
> 来源：[[锁机制与并发原语]]

---

## 线程池的任务执行流程？
?
1. 提交任务时，先判断核心线程数是否已满
2. 未满 → 创建核心线程执行
3. 已满 → 任务放入工作队列
4. 队列也满了 → 创建非核心线程（不超过 maximumPoolSize）
5. 都满了 → 执行拒绝策略
> 来源：[[锁机制与并发原语]]

---

## 虚线程（Virtual Thread）为什么不涉及操作系统内核切换？
?
虚线程的调度在 JVM 层面实现（用户态调度）：
1. 将阻塞操作转化为 JVM 内部的状态切换
2. OS 看到的载体线程（平台线程）始终处于运行态
3. 规避了昂贵的系统调用和 CPU 环切换
4. 适合 I/O 密集型场景，不适合 CPU 密集型
> 来源：[[锁机制与并发原语]]

---

## 虚线程的"固定（Pinning）"现象是什么？
?
当虚线程执行 synchronized 块或 Native 方法时，会被固定在载体线程上。
- 如果此时发生 I/O 阻塞，载体线程也会被阻塞
- 导致扩展性下降（一个载体线程只能跑一个虚线程）
- JDK 21+ 已部分优化：synchronized 不再固定，但 Native 方法仍会
> 来源：[[锁机制与并发原语]]

---

## 为什么在微服务中 Parallel GC 可能导致服务被剔除？
?
Parallel GC 的 Full GC 停顿时间较长，可能导致容器的 Liveness/Readiness Probe 超时，K8s 误判服务宕机并不断重启实例。
建议使用 G1 或 ZGC 替代。
> 来源：[[面试问题记录]]
