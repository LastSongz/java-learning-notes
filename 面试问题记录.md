 #flashcards/面试问题记录 

在重构万行级存储过程时，如何保证逻辑迁移的正确性（防回滚策略）
?
答案：
1. **静态分析**：SQLFlow/手工梳理逻辑分支。
2. **影子运行 (Shadowing)**：在 Java 层实现相同逻辑，异步调用并比对 SQL 结果与 Java 结果，记录 Diff。
3. **绞杀者模式**：按业务模块逐个剥离，通过 ACL (防腐层) 与旧库通信。
4. **场景测试**：基于生产采样数据构建全路径回归测试。
<!--SR:!2026-04-14,1,208-->

Q: 为什么“双向消息确认”不是解决分布式事务的最佳实践
?
1. **无限递归**：确认消息本身也可能丢失，导致发送方状态悬挂。
2. **复杂度高**：增加了网络开销和系统耦合。
3. **优化方案**：采用 **Outbox Pattern**（本地事务表）配合 **幂等消费**，或者使用 **Saga 模式** 进行最终一致性补偿。
<!--SR:!2026-04-14,1,208-->

Q: 如何避免 DDD 落地时写成“伪充血模型”
?
1. **识别聚合根**：确保业务操作只能通过聚合根进行，维护内部一致性。
2. **区分 Domain Service 与 Entity**：跨实体的复杂协调逻辑放入 Domain Service，单一实体的状态变更放入 Entity。
3. **隔离副作用**：Repository 接口定义在领域层，实现留在基础设施层，确保领域逻辑不依赖数据库特性（如触发器）。
<!--SR:!2026-04-14,1,208-->

## Q: DDD 中聚合根 (Aggregate Root) 的核心职责是什么
?
A:
- **核心职责**：维护业务不变量 (Invariants) 和确保事务一致性边界。
- **误区补丁**：不仅是管理生命周期，更重要的是通过封装确保边界内的数据永远处于合法状态。
- **优化点**：外部只能通过 ID 引用聚合根，严禁直接访问聚合内部成员。
<!--SR:!2026-04-14,1,208-->

## Q: 绞杀者模式 (Strangler Fig) 迁移存储过程的关键步骤
?
A:
1. **建立 ACL (防腐层)**：隔离新旧模型，避免 Oracle 存储过程的表结构污染新微服务模型。
2. **场景测试驱动**：通过 sqlFlow/静态分析识别分支，建立金丝雀测试。
3. **读写分离灰度**：
   - 读操作：新旧对比。
   - 写操作：旧逻辑主导 -> 影子运行对比 -> 业务开关切换 -> 彻底下线旧逻辑。
<!--SR:!2026-04-14,1,208-->

## Q: 跨聚合的一致性如何在高并发/实时性场景下权衡
?
A:
- **常规方案**：Transactional Outbox + MQ (最终一致性)。
- **高实时场景**：
  - 1. 重新审视边界：是否应属于同一个聚合?
  - 2. Saga 模式：通过编排或协同实现补偿。
  - 3. 避免暴力合并：合并聚合会导致数据库锁竞争加剧，破坏扩展性。
<!--SR:!2026-04-14,1,208-->

为什么在微服务中 Parallel GC 可能导致服务被剔除
?
**Answer**: 因为 Parallel GC 的 Full GC 停顿时间较长，可能导致容器的心跳检查（Liveness/Readiness Probe）超时，从而导致 K8s 误判服务宕机并不断重启实例。
<!--SR:!2026-04-15,2,230-->

内存分配热点图 (Flame Graph) 相比 Heap Dump 的优势
?
** **Answer**: Flame Graph 展示的是“哪里在产生对象”，而 Dump 展示的是“哪里留存了对象”。对于频繁分配导致频繁 Minor GC 的性能瓶颈，火焰图更有效。
<!--SR:!2026-04-15,2,230-->

 为什么 LongAdder 的 sum() 方法不保证强一致性
 ?
 ** **Answer**: 因为 sum() 执行时没有加全局锁，此时其他线程可能正在修改 Cell。它返回的是一个“统计学意义上的近似值”，适用于监控、计费等允许微小滞后的场景，不适用于对账。
<!--SR:!2026-04-14,1,228-->

 什么是 Sentinel 的自适应限流（Adaptive Flow Control）
 ?
 ** **Answer**: 它根据系统的 CPU 使用率、Load、RT（响应时间）等指标动态调整限流阈值，在系统接近崩溃边缘时自动限流，实现“自动化驾驶”式的系统保护。
<!--SR:!2026-04-14,1,210-->

> **Card 1: 为什么虚线程不涉及操作系统内核切换
 ?
**Answer**: 因为虚线程的调度是在 JVM 层面实现的（用户态调度）。它将阻塞操作转化为 JVM 内部的状态切换，OS 看到的载体线程（平台线程）始终处于运行态，从而规避了昂贵的系统调用和 CPU 环切换。
<!--SR:!2026-04-15,2,230-->

> **Card 2: 什么是虚线程的“固定 (Pinning)”现象？** 
> ?
> **Answer**: 当虚拟线程在执行 `synchronized` 块或原生方法（Native Method）时，它会被固定在载体线程上。如果此时发生 IO 阻塞，载体线程也会被阻塞，导致扩展性下降。

Q: 在集成外部异步事件（如 3PL 回调）时，如何处理业务逻辑上的状态乱序与节点跳跃
?
A: 不能使用强依赖前置状态的刚性状态机，也不能要求外部维护自增版本号。 应该使用**基于权重的优先级状态矩阵 (Priority Matrix)**：
1. 为每个业务状态分配权重（单调递增的值）。
2. 比较事件携带的状态权重与系统当前主记录的状态权重。
3. 仅当 `新事件权重 > 当前状态权重` 时，才向前推进主记录的状态，即使中间有节点丢失也能兼容。
<!--SR:!2026-04-14,1,222-->

Q: 实现 API 幂等性的最佳工程实践（第一道防线）是什么
?
A: 利用 **去重表 (Dedup Table)** 模式：
1. 从请求中提取确定性要素 (如: Provider + Entity_ID + Event_Type + Timestamp) 生成唯一的 Idempotency Key。
2. 利用数据库的 Unique Constraint 保证并发安全。
3. 捕获 DuplicateKeyException，**中断后续业务逻辑，但必须向上游返回成功 (ACK)**，以中断重试风暴。
<!--SR:!2026-04-14,1,222-->

Q: 如何保证数据库更新与外部副作用（如发短信、调用微服务）的最终一致性
?
A: 使用 **Transactional Outbox Pattern (事务性离群消息模式)**：
1. 在同一个本地数据库事务中，既更新业务数据，又在 `outbox` 表中插入一条任务记录。
2. 使用异步进程（或消息队列）读取 `outbox` 表并执行外部调用。
3. 确保外部调用成功后，才更新任务状态或删除任务。
4. 这种方式避免了 2PC 的复杂性，同时保证了 Side Effects 的“至少交付一次”。
<!--SR:!2026-04-14,1,222-->

Q: 为什么在 CDC (Change Data Capture) 同步中必须使用 ES 的 external_version
?
A: 因为分布式环境下的消息投递（如 Kafka/Flink）只能保证“至少交付一次”。
1. 网络抖动可能导致 binlog 事件重发。
2. 即使 Flink 保证顺序，ES 分片写入速度差异也可能导致旧数据覆盖新数据。 使用 `external_version`（如 DB 维护的版本号或时间戳）可以让 ES 引擎自动丢弃过期版本，确保最终一致性。
<!--SR:!2026-04-14,1,222-->

Q: 在 DDD 场景下，CDC 同步面临的最大挑战是什么
?
A: **模型阻抗失配**。 CDC 监听的是物理表结构，而搜索模型（ES Document）通常是业务聚合逻辑。 当聚合跨越多个物理表甚至微服务时，单纯靠 CDC 监听单表会导致：
1. 难以构建完整的聚合文档。
2. 同步逻辑中掺杂了大量本应属于领域层的计算逻辑（导致逻辑泄露）。
3. **建议：** 简单模型用 CDC，复杂聚合用领域事件驱动同步。
<!--SR:!2026-04-14,1,222-->

Q: 在高并发场景下，使用“领域事件 + 回查 API”同步 ES 有什么风险
?
如何优化？ A: 风险：回查 API 会给生产库造成二次压力（Read Amplification），可能导致数据库连接池耗尽。 优化手段： 1. **事件富化 (Enrichment)**：在事件中携带足够的关键字段，减少回查次数。 2. **读写分离**：回查操作指向只读从库 (Slave DB)。 3. **缓冲回查**：利用消息队列的延迟消费或批处理，将多次小的回查合并为一次批量查询。
<!--SR:!2026-04-14,1,222-->