---
title: "闪卡 - 中间件"
created: 2026-05-19
updated: 2026-05-19
tags:
  - 类型/闪卡
  - 分类/中间件
  - 主题/redis
  - 主题/mq
status: complete
category: middleware
---

 #flashcards/中间件

## Redis 为什么用单线程还能这么快？
?
1. **纯内存操作**：数据存在内存，读写极快
2. **IO 多路复用**：epoll 实现高并发连接处理
3. **避免线程切换开销**：无需加锁、无上下文切换
4. **高效的数据结构**：SDS、ziplist、skiplist 等针对场景优化
> 来源：[[中间件专题-Redis与MQ]]

---

## Redis 缓存穿透、击穿、雪崩分别是什么？怎么解决？
?
- **穿透**：查询不存在的数据 → 缓存空值 / 布隆过滤器
- **击穿**：热点 Key 过期瞬间大量请求打到 DB → 互斥锁 / 逻辑过期
- **雪崩**：大量 Key 同时过期或 Redis 宕机 → 过期时间加随机值 / 多级缓存 / 熔断降级
> 来源：[[中间件专题-Redis与MQ]]

---

## RDB 和 AOF 持久化有什么区别？
?
| 对比项 | RDB | AOF |
|--------|-----|-----|
| 原理 | 定时快照 | 追加写命令 |
| 恢复速度 | 快 | 慢 |
| 数据安全 | 可能丢失最后一次快照后的数据 | 最多丢 1 秒（everysec） |
| 文件体积 | 小（压缩） | 大（需重写压缩） |
| 适用场景 | 备份/灾难恢复 | 数据安全要求高 |

推荐：RDB 定时备份 + AOF everysec 刷盘（混合持久化）
> 来源：[[中间件专题-Redis与MQ]]

---

## Redis 哨兵模式的故障转移流程？
?
1. **主观下线（sdown）**：单个哨兵认为主库 down
2. **客观下线（odown）**：多数哨兵认为主库 down
3. **选举领头哨兵**（Raft 协议）
4. **选出新主库**：优先级最高 → offset 最新 → runId 最小
5. **其他从库指向新主库**
6. **旧主库恢复后降级为从库**
> 来源：[[中间件专题-Redis与MQ]]

---

## Redis Cluster 为什么用 16384 个槽位而不是一致性哈希？
?
1. 一致性哈希只有顺时针第一个节点负责数据，容易数据倾斜
2. 16384 = 2^14，既能保证节点均匀分布
3. 槽位数量不会产生过多心跳包（Gossip 协议通信）
4. 键通过 `CRC16(key) % 16384` 映射到槽位
> 来源：[[中间件专题-Redis与MQ]]

---

## 多级缓存如何保证数据一致性？
?
采用 Cache Aside + Binlog 同步方案：
1. 业务服务只负责更新数据库
2. 通过 Canal 或 FlinkCDC 监听 Binlog 变更
3. 发送 MQ 通知缓存同步服务
4. 同步服务失效/更新 Redis 和本地缓存
5. 配合缓存过期时间兜底
> 来源：[[中间件专题-Redis与MQ]]

---

## RocketMQ 的事务消息是如何实现的？
?
1. 生产者发送半消息（Half Message）到 Broker（对消费者不可见）
2. 执行本地事务
3. 根据本地事务结果二次提交（Commit/Rollback）
4. 如果 Broker 长时间没收到二次确认，回查生产者事务状态
> 来源：[[面试问题记录]]

---

## Kafka、RocketMQ、RabbitMQ 如何选型？
?
| 维度 | Kafka | RocketMQ | RabbitMQ |
|------|-------|----------|----------|
| 吞吐量 | 极高（百万级） | 高（十万级） | 中（万级） |
| 延迟 | ms 级 | ms 级 | μs 级 |
| 事务消息 | 不支持 | 支持 | 支持 |
| 适用场景 | 日志/大数据 | 电商/金融 | 中小项目 |
| 社区活跃度 | 高 | 中 | 高 |
> 来源：[[中间件专题-Redis与MQ]]
