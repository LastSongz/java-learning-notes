---
created: 2026-04-27
tags: #运达 #面试专区 #状态/优先准备 #moc
---

# 🎯 运达公司面试专区

> 针对**运达公司**面试准备的5大核心知识点速查入口
> 每个知识点包含：专题笔记（深度学习）+ 速查要点（快速回顾）

---

## ⚡ 速查索引（一眼找到）

| #   | 知识点           | 专题笔记                          | 核心速查                           |
| --- | ------------- | ----------------------------- | ------------------------------ |
| 1   | JVM内存结构       | [[JVM内存结构]]                   | 运行时数据区、堆分代、JMM、GC调优            |
| 2   | 中间件(Redis/MQ) | [[中间件专题-Redis与MQ]]            | Redis缓存问题、MQ选型与可靠性             |
| 3   | HashMap/CHM   | [[HashMap与ConcurrentHashMap]] | 底层原理、扩容、线程安全、锁粒度               |
| 4   | 锁/CAS/AQS     | [[锁机制与并发原语]]                  | synchronized锁升级、AQS原理、volatile |
| 5   | MVCC          | [[MVCC多版本并发控制]]               | 隐藏字段、Undo Log、Read View        |

---

## 1️⃣ JVM内存结构和简述

> 📄 完整笔记：[[JVM内存结构]]

**30秒速记：**
- **线程私有**：程序计数器（唯一不OOM）、虚拟机栈（StackOverflow/OOM）、本地方法栈
- **线程共享**：堆（Young = Eden + S0/S1，Old）、元空间（JDK8替代永久代）
- **JMM三特性**：可见性（volatile）、原子性（CAS/锁）、有序性（happens-before）
- **GC核心**：G1 > CMS（无碎片、可预测停顿），Full GC排查：top → jstack → MAT

**运达可能的追问方向：**
- 线上 CPU 100% 怎么排查？
- G1 和 CMS 区别？
- 逃逸分析和栈上分配？

---

## 2️⃣ MQS、Redis 等中间件

> 📄 完整笔记：[[中间件专题-Redis与MQ]]

**30秒速记：**
- **Redis三大问题**：穿透（布隆过滤器）、击穿（互斥锁）、雪崩（随机过期）
- **Redis为什么快**：单线程 + epoll IO多路复用 + 内存操作 + 避免上下文切换
- **MQ选型**：RocketMQ（事务消息、金融级）vs Kafka（高吞吐、日志场景）vs RabbitMQ（通用）
- **消息可靠性**：生产者确认 + Broker持久化 + 消费者手动ACK + 幂等消费
- **消息积压**：扩容消费者 + 临时Queue + 批量消费

**运达可能的追问方向：**
- Redis和数据库双写一致性怎么保证？
- RocketMQ事务消息原理？
- 分布式锁（Redisson）看门狗机制？

---

## 3️⃣ HashMap、ConcurrentHashMap

> 📄 完整笔记：[[HashMap与ConcurrentHashMap]]

**30秒速记：**
- **HashMap结构**：数组 + 链表 + 红黑树（JDK8），容量2的幂（hash & len-1 位运算）
- **扩容**：size > cap * 0.75 触发，新容量 × 2，JDK8尾插法避免死循环
- **线程不安全**：JDK7头插法死循环、JDK8数据覆盖
- **CHM JDK7**：Segment分段锁（ReentrantLock），并发度=Segment数
- **CHM JDK8**：CAS + synchronized（锁桶节点），并发度=桶数量，支持多线程并发扩容
- **红黑树阈值8**：泊松分布，链表≥8概率约千万分之一

**运达可能的追问方向：**
- HashMap为什么容量是2的幂次？
- CHM为什么不允许null key/value？（二义性）
- JDK8 CHM的put流程？

---

## 4️⃣ 锁：CAS、synchronized、ReentrantLock、AQS

> 📄 完整笔记：[[锁机制与并发原语]]

**30秒速记：**
- **CAS**：CPU原子指令（lock cmpxchg），ABA问题用 AtomicStampedReference
- **synchronized锁升级**：无锁 → 偏向锁（线程ID）→ 轻量级锁（自旋）→ 重量级锁（Monitor阻塞）
- **ReentrantLock vs synchronized**：可中断、可超时、公平/非公平、多条件变量
- **AQS**：volatile state + CLH双向队列，独占/共享模式，支撑 ReentrantLock/Semaphore/CountDownLatch
- **volatile**：可见性（lock前缀）+ 禁止重排序（内存屏障），不保证原子性

**运达可能的追问方向：**
- synchronized锁升级全过程？
- AQS为什么用双向链表？
- 偏向锁JDK15为什么默认关闭？
- DCL单例为什么需要volatile？

---

## 5️⃣ MVCC（多版本并发控制）

> 📄 完整笔记：[[MVCC多版本并发控制]]

**30秒速记：**
- **三个组件**：隐藏字段（DB_TRX_ID/DB_ROLL_PTR/DB_ROW_ID）+ Undo Log + Read View
- **快照读 vs 当前读**：普通select不加锁（快照读），for update加锁（当前读）
- **Read View可见性**：creator_trx_id → min_trx_id → max_trx_id → m_ids 逐级判断
- **RC vs RR**：RC每次快照读生成新Read View，RR只在第一次生成（所以RR可重复读）

**运达可能的追问方向：**
- MVCC能解决幻读吗？（RR级别下快照读可以，当前读需要间隙锁）
- Undo Log什么时候清理？（purge线程）
- MySQL 8.0 MVCC有什么改进？

---

## 📎 关联资源

- [[MOC-Java面试]] - 面试知识体系总导航
- [[MOC-Java学习]] - Java技术学习导航
- [[简历知识点总结]] - 核心面试知识大全（47KB）
- [[面试问题记录]] - 闪卡复习
- [[简历涉及知识点针对性梳理]] - Flashcard格式知识点

---

## 📊 学习状态

| 知识点         | 笔记状态       | 复习状态  |
| ----------- | ---------- | ----- |
| JVM内存结构     | ✅ 已新建      | ⬜ 待复习 |
| Redis/MQ中间件 | ✅ 已新建      | ⬜ 待复习 |
| HashMap/CHM | ✅ 已新建      | ⬜ 待复习 |
| 锁/CAS/AQS   | ✅ 已新建      | ⬜ 待复习 |
| MVCC        | ✅ 已有（199行） | ⬜ 待复习 |
