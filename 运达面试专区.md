---
title: "运达公司面试专区"
created: 2026-04-27
updated: 2026-06-24
tags:
  - 运达
  - 面试专区
  - 类型/moc
status: complete
category: interview
---

# 运达公司面试专区

> 针对运达公司面试准备的 6 大核心知识点速查入口。
> 每个知识点都尽量串成“专题笔记 + 30 秒速记 + 可能追问”的结构，便于临场切换。

---

## ⚡ 速查索引

| #   | 知识点           | 专题笔记                          | 核心速查                          |
| --- | ------------- | ----------------------------- | ----------------------------- |
| 1   | JVM内存结构       | [[JVM内存结构]]                   | 运行时数据区、堆分代、JMM、GC调优           |
| 2   | 中间件（Redis/MQ） | [[中间件专题-Redis与MQ]]            | 缓存问题、MQ选型、可靠性与幂等              |
| 3   | HashMap/CHM   | [[HashMap与ConcurrentHashMap]] | 底层原理、扩容、线程安全、锁粒度              |
| 4   | 锁/CAS/AQS     | [[锁机制与并发原语]]                  | synchronized、volatile、CAS、AQS |
| 5   | MVCC          | [[MVCC多版本并发控制]]               | Undo Log、Read View、RC/RR、当前读  |
| 6   | 复用件平台/能力中台    | [[复用件平台产品理解]]                 | 平台定位、边界、交付形态、MVP 闭环           |

---

## 1. JVM内存结构

> 📄 完整笔记：[[JVM内存结构]]

**30 秒速记：**
- 线程私有：程序计数器、虚拟机栈、本地方法栈。
- 线程共享：堆、元空间；JDK8 之后元空间替代永久代。
- JMM 三特性：可见性、原子性、有序性。
- Full GC 排查主线：`top/jps -> jstack -> jmap/MAT -> 结合业务日志定位`。

**运达可能的追问方向：**
- 线上 CPU 100% 怎么排查？
- G1 和 CMS 的取舍是什么？
- 逃逸分析、栈上分配能解决什么问题？

---

## 2. Redis、MQ 等中间件

> 📄 完整笔记：[[中间件专题-Redis与MQ]]

**30 秒速记：**
- Redis 三大问题：穿透、击穿、雪崩。
- Redis 为什么快：内存操作、单线程事件循环、IO 多路复用、减少上下文切换。
- MQ 选型：RocketMQ 偏事务与可靠性，Kafka 偏吞吐与日志场景，RabbitMQ 偏通用路由。
- 消息可靠性要拆成：生产者确认、Broker 持久化、消费者 ACK、消费幂等。
- 消息积压排查要区分：上游突发、消费者慢、单分区热点、下游故障。

**运达可能的追问方向：**
- Redis 和数据库双写一致性怎么做？
- RocketMQ 事务消息原理是什么？
- Redisson 看门狗为什么能续期？有什么边界？

---

## 3. HashMap、ConcurrentHashMap

> 📄 完整笔记：[[HashMap与ConcurrentHashMap]]

**30 秒速记：**
- HashMap：数组 + 链表 + 红黑树，容量通常取 2 的幂。
- 扩容触发：`size > capacity * loadFactor`，JDK8 用尾插避免死循环。
- HashMap 线程不安全：并发写可能导致覆盖、丢数据，JDK7 还可能死循环。
- CHM JDK7：Segment 分段锁；CHM JDK8：CAS + synchronized 锁桶节点。
- 红黑树阈值 8 是为了在极端 hash 冲突时把查找退化控制住。

**运达可能的追问方向：**
- 为什么容量是 2 的幂？
- 为什么 CHM 不允许 `null key/value`？
- JDK8 `put` 流程里哪些地方用了 CAS，哪些地方用了锁？

---

## 4. 锁、CAS、synchronized、AQS

> 📄 完整笔记：[[锁机制与并发原语]]

**30 秒速记：**
- CAS 依赖 CPU 原子指令，常见问题是 ABA，可用版本号规避。
- `synchronized` 关注锁升级链路，而不是只背“重量级锁”。
- `ReentrantLock` 的优势：可中断、可超时、公平锁、多个条件队列。
- AQS 主体是 `state + CLH 双向队列`，是很多同步器的骨架。
- `volatile` 解决可见性和重排序，不解决复合操作原子性。

**运达可能的追问方向：**
- synchronized 锁升级全过程怎么讲？
- AQS 为什么用双向链表？
- 偏向锁在新版本 JDK 里的变化说明了什么？
- DCL 单例为什么一定要 `volatile`？

---

## 5. MVCC（多版本并发控制）

> 📄 完整笔记：[[MVCC多版本并发控制]]

**30 秒速记：**
- MVCC 三件套：隐藏字段、Undo Log、Read View。
- 快照读不加锁，当前读会拿锁。
- Read View 关键是用活跃事务列表判断当前版本是否可见。
- RC 每次快照读都可能生成新 Read View；RR 通常只在第一次生成。

**运达可能的追问方向：**
- MVCC 能不能解决幻读？
- Undo Log 什么时候清理？
- RC 和 RR 在业务上的真实差别是什么？

---

## 6. 复用件平台与能力中台

> 📄 完整笔记：[[复用件平台产品理解]]

**30 秒速记：**
- 平台定位不是 Maven 私仓，也不是低代码，而是“组件市场 + 复用治理 + 接入规范平台”。
- 复用对象要区分：技术组件、算法能力、工程模板、业务能力、集成规范。
- 交付形态要区分：Jar/Starter、API、脚手架、SQL、Zip/配置包、前端组件、镜像。
- MVP 重点不是铺大摊子，而是先跑通 Java 组件发布、审核、订阅、接入的闭环。
- 面试表达重点：说明边界、治理价值、为什么先做 Java 生态、为什么不一次性做成“万能平台”。

**运达可能的追问方向：**
- 复用件平台和 Maven 私仓到底有什么区别？
- 为什么第一阶段先做 Java Jar/Starter，而不是直接做业务模块大而全平台？
- `component.yaml` 在治理里承担什么角色？

---

## 📎 关联资源

- [[运达后端面试准备清单]] - 公司、岗位、项目表达的总清单
- [[复用件平台产品理解]] - 运达平台化话题的专项展开
- [[MOC-Java面试]] - 面试知识体系总导航
- [[MOC-Java学习]] - Java 技术学习导航
- [[简历知识点总结]] - 核心面试知识大全
- [[面试问题记录]] - 闪卡复习
- [[简历涉及知识点针对性梳理]] - 针对简历知识点强化

---

## 📊 学习状态

| 知识点 | 笔记状态 | 复习状态 |
| --- | --- | --- |
| JVM内存结构 | ✅ 已建专题 | ⬜ 待复习 |
| Redis/MQ 中间件 | ✅ 已建专题 | ⬜ 待复习 |
| HashMap/CHM | ✅ 已建专题 | ⬜ 待复习 |
| 锁/CAS/AQS | ✅ 已建专题 | ⬜ 待复习 |
| MVCC | ✅ 已建专题 | ⬜ 待复习 |
| 复用件平台/能力中台 | ✅ 已建专题 | ⬜ 待复习 |

## 使用建议

1. 面试前 10 分钟先看上面的速查索引和 30 秒速记。
2. 如果被追问业务平台化，优先跳到 [[复用件平台产品理解]]。
3. 如果需要完整复盘公司理解和自我表达，再回到 [[运达后端面试准备清单]]。
