---
tags: #java #并发 #锁 #面试 #运达
---

# 锁机制与并发原语

## 概述

并发编程是 Java 技术栈中最核心的能力之一，而锁机制是解决并发问题的基石。本篇涵盖 CAS、synchronized、ReentrantLock、AQS、volatile 等所有高频面试知识点，适用于运达公司等中高级 Java 岗位面试准备。

---

## 一、CAS（Compare And Swap）

### 1.1 原理概述

CAS 是 CPU 提供的原语级别操作，全称 Compare And Swap（比较并交换）。其核心思想是：**将内存中的值 V 与期望的旧值 A 进行比较，如果相等则将内存值更新为新值 B，整个操作原子完成**。

三个操作数缺一不可：
- **V**：内存中当前实际值
- **A**：旧的预期值（修改前的快照）
- **B**：需要写入的新值

### 1.2 底层实现

在 JDK 底层，CAS 依赖 `Unsafe` 类实现。Unsafe 是 JDK 内部提供的直接操作内存的类，Java 标准库中的原子类（如 AtomicInteger）都基于它实现。

```java
// AtomicInteger 内部实现（简化）
public final int incrementAndGet() {
    return unsafe.getAndAddInt(this, valueOffset, 1) + 1;
}

// Unsafe 类中的本地方法
public final int getAndAddInt(Object o, long offset, int delta) {
    int v;
    do {
        v = getIntVolatile(o, offset);  // 读取当前值
    } while (!compareAndSwapInt(o, offset, v, v + delta));  // CAS 更新
    return v;
}
```

在 CPU 层面，CAS 通过 `lock cmpxchg` 指令实现。当执行 CAS 时，CPU 会锁定数据总线，确保操作的原子性。

### 1.3 CAS 三大问题

#### 问题一：ABA 问题

**问题描述**：线程 T1 读取内存值 A，准备更新为 C。但在 T1 执行 CAS 期间，另一个线程 T2 将值从 A 改为 B 又改回 A。T1 看到值仍是 A，认为没有变化就直接更新了。

**解决方案**：
- **AtomicStampedReference**：维护一个版本号，每次修改连带版本号一起更新
- **AtomicMarkableReference**：维护一个布尔标记位，用于判断是否被修改过

```java
// AtomicStampedReference 使用示例
AtomicStampedReference<Integer> ref = new AtomicStampedReference<>(100, 0);
int[] stamp = new int[1];
Integer oldValue = ref.get(stamp);  // 获取值和版本号
ref.compareAndSet(oldValue, 200, stamp[0], stamp[0] + 1);
```

#### 问题二：自旋开销

**问题描述**：当 CAS 长时间不成功时（例如高并发场景），线程会一直空循环检查，导致 CPU 资源浪费。

**适用场景**：CAS 适用于竞争不激烈、只涉及单个共享变量的场景。在竞争激烈的情况下，自旋会显著影响性能。

#### 问题三：多变量原子操作

**问题描述**：CAS 只能保证单个共享变量的原子操作，无法直接扩展到多个变量。

**解决方案**：使用 `AtomicReference` 将多个变量封装为一个对象，通过对这个引用进行 CAS 操作来保证原子性。

```java
class User {
    String name;
    int age;
}

AtomicReference<User> userRef = new AtomicReference<>(new User("张三", 25));
userRef.compareAndSet(oldUser, new User("李四", 30));
```

### 1.4 应用场景

| 原子类 | 适用场景 | 特点 |
|--------|----------|------|
| AtomicInteger | 计数器、统计 | 基础原子整数 |
| AtomicLong | 计数、ID 生成 | 64 位值原子操作 |
| LongAdder | 高并发统计 | 分段 CAS，显著提升吞吐量 |

**LongAdder 原理**：在高并发场景下，AtomicLong 的 CAS 操作会严重竞争。LongAdder 将变量拆分为多个段（Cell），每个段独立 CAS，最后求和。分段数量与 CPU 核数相关，核数越多分段越多，并发能力越强。

---

## 二、synchronized

### 2.1 基本用法

synchronized 是 Java 内置的互斥锁，采用悲观锁策略。按照锁的粒度分两类：

**对象锁**：作用于实例对象
```java
// 同步代码块
synchronized (this) {
    // 临界区
}

// 同步方法
public synchronized void method() {
    // 临界区
}
```

**类锁**：作用于 Class 对象
```java
// 同步静态方法
public static synchronized void staticMethod() {
    // 临界区
}

// 同步代码块（Class 对象）
synchronized (MyClass.class) {
    // 临界区
}
```

### 2.2 底层原理

#### 同步代码块

编译后字节码中会插入 `monitorenter` 和 `monitorexit` 指令：

```java
public void method() {
    synchronized (this) {
        System.out.println("hello");
    }
}

// 字节码（简化）
// monitorenter  // 进入同步块，获取 monitor 锁
// aload_0
// getfield #2
// iconst_1
// monitorexit   // 退出同步块，释放 monitor 锁
```

#### 同步方法

同步方法的字节码表现为 `ACC_SYNCHRONIZED` 标志位。方法调用时，JVM 会检查这个标志，自动获取对象锁。

### 2.3 锁升级全过程

这是 synchronized 最核心的面试点。JVM 对 synchronized 实现了四种锁状态，逐步升级，不可降级：

#### 阶段一：无锁 → 偏向锁

**适用场景**：单线程访问，无竞争。

**原理**：Mark Word（对象头）存储线程 ID。第一次 CAS 获取锁时，将线程 ID 写入 Mark Word。之后同一线程进入同步块，只需简单检查 Mark Word 中的线程 ID 是否为当前线程。

**特点**：第一次 CAS 成本较高，但之后无任何额外开销。

#### 阶段二：偏向锁 → 轻量级锁

**触发条件**：出现竞争，偏向锁被撤销。

**撤销过程**：
1. 到达安全点（safepoint），暂停持有偏向锁的线程
2. 检查线程是否存活、是否在同步块内
3. 如果不在，撤销偏向锁，升级为无锁状态
4. 如果在，升级为轻量级锁

**轻量级锁原理**：
- 线程在栈帧中创建 Lock Record
- Mark Word 复制到 Lock Record
- Mark Word 通过 CAS 指向栈中的 Lock Record
- 其他线程自旋等待，不阻塞

#### 阶段三：轻量级锁 → 重量级锁

**触发条件**：自旋超过阈值（JVM 自适应调整）。

**重量级锁原理**：
- 线程不再自旋，膨胀为重量级锁
- 依赖 Monitor 对象（操作系统 mutex）实现
- 未获取锁的线程被阻塞（Linux pthread_mutex），上下文切换开销大

**锁状态对比**：

| 锁状态 | 标志位 | 存储内容 | 适用场景 | 优点 | 缺点 |
|--------|--------|----------|----------|------|------|
| 无锁 | 001 | 对象哈希码等 | 无竞争 | | |
| 偏向锁 | 101 | 线程 ID | 单线程重入 | 获取锁代价极低 | 多线程竞争需撤销 |
| 轻量级锁 | 00 | 指向栈帧的指针 | 多线程交替执行 | 避免线程阻塞 | 自旋消耗 CPU |
| 重量级锁 | 10 | 指向 Monitor 的指针 | 多线程激烈竞争 | 线程不消耗 CPU | 线程阻塞，上下文切换 |

### 2.4 锁消除

JIT 编译器在运行时进行逃逸分析（Escape Analysis），如果判断某个锁对象不会逃逸出当前线程（不会被其他线程访问），就会消除这个锁。

```java
public void method() {
    // StringBuffer 不会逃逸，JIT 会消除 synchronized
    StringBuffer sb = new StringBuffer();
    sb.append("a");
    sb.append("b");
    // sb 仅为方法局部变量，不可能被其他线程访问
}
```

### 2.5 锁粗化

JIT 编译器会将多次相邻的锁操作合并为一次，减少锁的获取和释放次数。

```java
// 优化前
StringBuffer sb = new StringBuffer();
synchronized (sb) {
    sb.append("a");
}
synchronized (sb) {
    sb.append("b");
}
synchronized (sb) {
    sb.append("c");
}

// JIT 优化后
synchronized (sb) {
    sb.append("a");
    sb.append("b");
    sb.append("c");
}
```

### 2.6 偏向锁在 JDK15 后默认关闭

JDK15 开始，默认关闭偏向锁（-XX:+UseBiasedLocking 默认值为 false）。原因如下：

1. **竞争撤销成本高**：偏向锁在多线程竞争时需要撤销，成本仅次于膨胀为重量级锁
2. **现代应用多为多线程**：纯单线程场景较少，偏向锁命中率低
3. **JIT 优化替代**：JIT 编译器已经足够智能，偏向锁的价值下降

---

## 三、ReentrantLock

### 3.1 基本特性

ReentrantLock 是 JDK 5 提供的显式锁，需要手动 `lock()` 和 `unlock()`。相比 synchronized 的核心优势：

| 特性 | 说明 |
|------|------|
| 可重入 | 同一线程可多次获取锁 |
| 可中断 | lockInterruptibly() 可响应中断 |
| 可超时 | tryLock(timeout) 防止死锁 |
| 公平/非公平 | 支持公平锁和非公平锁 |

### 3.2 公平锁 vs 非公平锁

**公平锁（FairSync）**：按请求顺序获取锁，FIFO 队列管理。

**非公平锁（NonfairSync）**：新线程可直接尝试获取锁（可能插队）。

```java
// 创建公平锁
ReentrantLock fairLock = new ReentrantLock(true);

// 创建非公平锁（默认）
ReentrantLock unfairLock = new ReentrantLock(false);
```

**为什么非公平锁吞吐量更高？**

1. 线程切换有成本，非公平锁允许新线程立即尝试获取锁
2. 避免唤醒线程和申请线程之间的空档期重叠
3. 代价是可能造成线程饥饿

### 3.3 与 synchronized 对比

| 对比维度 | synchronized | ReentrantLock |
|----------|--------------|---------------|
| 实现层面 | JVM 内置，字节码层面 | JDK API，Java 代码实现 |
| 锁释放 | 自动释放（退出同步块） | 必须手动 unlock() |
| 可中断 | 否 | lockInterruptibly() 支持 |
| 超时控制 | 否 | tryLock() 支持 |
| 公平性 | 非公平（无法设置） | 可配置公平/非公平 |
| 条件变量 | Object.wait/notify | Condition.await/signal |
| 批量解锁 | 否 | unlock() 可指定次数 |

### 3.4 Condition 条件变量

Condition 提供了比 Object.wait/notify 更强大的等待/通知机制：

```java
ReentrantLock lock = new ReentrantLock();
Condition condition = lock.newCondition();

// 等待
lock.lock();
try {
    while (条件不满足) {
        condition.await();  // 释放锁并等待
    }
    // 业务逻辑
} finally {
    lock.unlock();
}

// 通知
lock.lock();
try {
    条件满足 = true;
    condition.signal();      // 唤醒一个线程
    // condition.signalAll();  // 唤醒所有等待线程
} finally {
    lock.unlock();
}
```

**与 Object.wait/notify 对比**：

| 特性 | Object.wait/notify | Condition |
|------|---------------------|-----------|
| 精准唤醒 | 否，一个 Monitor 只能有一个等待集 | 是，每个 Condition 独立等待集 |
| 多条件 | 需配合多个 synchronized | 可创建多个 Condition |
| 响应中断 | 不支持 | awaitUninterruptibly() 支持 |

### 3.5 ReentrantReadWriteLock

读写锁适用于读多写少场景：

| 模式 | 读读 | 读写 | 写写 |
|------|------|------|------|
| 读锁 | 共享 | 互斥 | 互斥 |
| 写锁 | 互斥 | 互斥 | 互斥 |

```java
ReentrantReadWriteLock rwLock = new ReentrantReadWriteLock();
Lock readLock = rwLock.readLock();
Lock writeLock = rwLock.writeLock();

// 读操作
readLock.lock();
try {
    // 读取共享资源
} finally {
    readLock.unlock();
}

// 写操作
writeLock.lock();
try {
    // 修改共享资源
} finally {
    writeLock.unlock();
}
```

**注意事项**：
- 读锁是共享锁，写锁是独占锁
- 写锁降级为读锁是允许的（持有写锁时获取读锁）
- 读锁升级为写锁是不允许的（会造成死锁）

### 3.6 StampedLock（JDK8）

StampedLock 引入了乐观读锁，进一步优化读多写少场景的性能：

```java
StampedLock sl = new StampedLock();

// 乐观读（不阻塞，写锁可能已获取）
long stamp = sl.tryOptimisticRead();
// 检查验证
if (!sl.validate(stamp)) {
    // 验证失败，升级为悲观读
    stamp = sl.readLock();
    try {
        // 读取
    } finally {
        sl.unlockRead(stamp);
    }
}

// 写锁
long stamp = sl.writeLock();
try {
    // 写入
} finally {
    sl.unlockWrite(stamp);
}
```

**三种模式对比**：

| 模式 | 特点 | 适用场景 |
|------|------|----------|
| 乐观读 | 不阻塞，性能最高 | 读多写少，写锁冲突少 |
| 悲观读 | 类似 ReentrantReadWriteLock 读锁 | 读写冲突较多 |
| 写锁 | 独占，阻塞所有读和写 | 写操作密集 |

---

## 四、AQS（AbstractQueuedSynchronizer）

### 4.1 核心结构

AQS 是 Java 并发包的基础，ReentrantLock、Semaphore、CountDownLatch 等都基于它实现。

**两大核心组件**：

1. **state（volatile int）**：同步状态，通过 CAS 操作原子修改
2. **CLH 队列**：双向 FIFO 队列，管理等待线程

```java
public abstract class AbstractQueuedSynchronizer extends AbstractOwnableSynchronizer {
    // 同步状态
    private volatile int state;

    // 等待队列头结点
    private transient volatile Node head;

    // 等待队列尾结点
    private transient volatile Node tail;

    // Node 内部类（双向链表结构）
    static final class Node {
        volatile int waitStatus;      // 等待状态
        volatile Node prev;          // 前驱节点
        volatile Node next;          // 后继节点
        Thread waiter;               // 等待线程
        int mode;                    // SHARED/EXCLUSIVE
    }
}
```

### 4.2 独占模式 vs 共享模式

| 模式 | 说明 | 代表实现 |
|------|------|----------|
| 独占模式（Exclusive） | 同一时刻只有一个线程能获取锁 | ReentrantLock |
| 共享模式（Shared） | 多个线程可同时获取锁 | Semaphore、CountDownLatch |

### 4.3 为什么用双向链表

单向链表无法满足 AQS 的需求：

1. **取消竞争**：线程等待过程中可能被中断，需要移除节点。双向链表可以从任意节点快速定位前驱节点
2. **唤醒机制**：释放锁时需要找到后继节点唤醒，双向链表可以直接操作
3. **公平性判断**：需要知道前驱节点的状态，判断是否轮到当前节点获取锁

### 4.4 源码核心流程

#### acquire(1) 独占模式获取

```java
public final void acquire(int arg) {
    // 1. 尝试获取锁
    if (!tryAcquire(arg)) {
        // 2. 获取失败，加入等待队列
        Node node = addWaiter(Node.EXCLUSIVE);
        // 3. 自旋等待唤醒
        if (acquireQueued(node, arg)) {
            // 4. 等待过程中被中断
            selfInterrupt();
        }
    }
}

final boolean acquireQueued(Node node, int arg) {
    boolean interrupted = false;
    for (;;) {
        // 获取前驱节点
        Node p = node.predecessor();
        // 如果前驱是头结点，尝试获取锁
        if (p == head && tryAcquire(arg)) {
            setHead(node);
            p.next = null;  // 帮助 GC
            return interrupted;
        }
        // 前驱不是头结点或获取失败，park 当前线程
        if (shouldParkAfterFailedAcquire(p, node))
            LockSupport.park(this);
        // 被中断唤醒
        if (Thread.interrupted())
            interrupted = true;
    }
}
```

#### release(1) 独占模式释放

```java
public final boolean release(int arg) {
    // 1. 尝试释放锁
    if (tryRelease(arg)) {
        Node h = head;
        // 2. 唤醒后继节点
        if (h != null && h.waitStatus != 0)
            unparkSuccessor(h);
        return true;
    }
    return false;
}

private void unparkSuccessor(Node node) {
    int ws = node.waitStatus;
    if (ws < 0)
        node.compareAndSetWaitStatus(ws, 0);
    // 找到后继节点
    Node s = node.next;
    if (s == null || s.waitStatus > 0) {
        // 后继为空或已取消，遍历找到最近的有效后继
        s = null;
        for (Node t = tail; t != null && t != node; t = t.prev)
            if (t.waitStatus <= 0)
                s = t;
    }
    if (s != null)
        LockSupport.unpark(s.thread);  // 唤醒线程
}
```

### 4.5 AQS 应用一览

| 并发组件 | 同步器类型 | state 含义 |
|----------|------------|------------|
| ReentrantLock | 独占 | 1=已获取，N=重入次数 |
| ReentrantReadWriteLock | 独占+共享 | 高16位读锁，低16位写锁 |
| Semaphore | 共享 | 剩余许可数 |
| CountDownLatch | 共享 | 初始计数，递减到0 |
| CyclicBarrier | 独占 | 等待线程数 |

---

## 五、volatile

### 5.1 可见性实现原理

volatile 通过强制刷回主内存和失效其他 CPU 缓存来实现可见性：

1. **写操作**：对 volatile 变量的写，会强制将值刷新回主内存
2. **读操作**：读取 volatile 变量时，强制从主内存读取最新值，并使其他 CPU 的缓存行失效

这依赖 CPU 的 `lock` 前缀指令实现（在 Pentium 4 之后的 CPU 中等价于内存屏障）。

### 5.2 禁止指令重排序

编译器优化可能对指令重排序。volatile 通过插入内存屏障，防止特定类型的重排序：

```java
// 示例
volatile boolean flag = false;

Thread A:
    doSomething();
    flag = true;  // 内存屏障禁止此行重排序到 doSomething() 之前

Thread B:
    if (flag) {
        doOtherThing();  // 一定能看到 A 执行的结果
    }
```

### 5.3 volatile 不保证原子性

以 `i++` 为例，这个操作分解为：
1. read：读取 i 到 CPU 寄存器
2. load：将值从寄存器加载到变量
3. use：CPU 使用这个值
4. assign：计算后赋值
5. store：存回寄存器
6. write：刷新回主内存

volatile 只保证 read-load-use-assign-store-write 序列中 load 和 write 是原子的，但中间的 use-assign-store 不是原子操作。所以多线程下 `i++` 会丢失更新。

**线程安全需要原子性保证的场景**：
- ❌ volatile int i; i++;（不安全）
- ✅ synchronized 或 AtomicInteger 的 incrementAndGet()（安全）

### 5.4 典型应用：DCL 单例模式

```java
public class Singleton {
    // 必须加 volatile
    private static volatile Singleton instance;

    public static Singleton getInstance() {
        if (instance == null) {           // 第一次检查
            synchronized (Singleton.class) {
                if (instance == null) {   // 第二次检查
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```

**为什么要 volatile？**

`instance = new Singleton()` 分解为：
1. 分配内存
2. 调用构造方法
3. 将地址赋值给 instance

编译器可能重排序为 1 → 3 → 2。如果没有 volatile，线程 A 可能看到 instance 非空但对象未构造完成，线程 B 直接使用这个未初始化对象会导致严重错误。

### 5.5 内存屏障详解

JSR-133 定义了四种内存屏障：

| 屏障类型 | 说明 | 举例 |
|----------|------|------|
| LoadLoad | 确保 Load1 在 Load2 之前读取 | 读volatile后读普通变量 |
| StoreStore | 确保 Store1 在 Store2 之前刷新 | 写普通后写volatile |
| LoadStore | 确保 Load1 在 Store2 之前完成 | 读volatile后写普通 |
| StoreLoad | 确保 Store1 在 Load2 之前刷新 | 写volatile后读普通（最重） |

StoreLoad 是最重的屏障，x86 架构下会使用 `lock prefix` 实现。

---

## 六、面试高频速查

### Q: synchronized 和 ReentrantLock 区别？

**答**：实现层面看，synchronized 是 JVM 内置锁，通过 monitorenter/monitorexit 字节码实现；ReentrantLock 是 JDK 提供的 API，通过 AQS 实现。synchronized 在退出同步块时自动释放锁，ReentrantLock 必须手动调用 unlock()。ReentrantLock 支持可中断（lockInterruptibly）、可超时（tryLock）、公平锁配置，而 synchronized 不支持。ReentrantLock 可绑定多个 Condition 条件变量实现精准唤醒，synchronized 只能使用 Object.wait/notify。在低竞争场景下两者性能差异不大，高并发场景 ReentrantLock 表现更优。

### Q: AQS 为什么用双向链表？

**答**：双向链表满足三个核心需求。第一，取消竞争时需要快速定位前驱节点，单向链表需要从头遍历，双向链表可直接通过 prev 指针找到。第二，释放锁时需要唤醒后继节点，双向链表可以直接操作 next 指针。第三，公平锁需要判断前驱节点是否为头结点来决定是否获取锁。双向链表使这些操作都是 O(1) 复杂度，单向链表则需要 O(n)。

### Q: 偏向锁、轻量级锁、重量级锁分别适用什么场景？

**答**：偏向锁适用于单线程重复访问同步块的场景，首次获取后同一线程进入无需任何 CAS 操作。轻量级锁适用于多线程交替执行同步块的场景，线程不阻塞而是通过自旋等待，竞争不激烈时性能好。重量级锁适用于多线程激烈竞争的场景，自旋超过阈值后膨胀，线程被阻塞不再消耗 CPU，但上下文切换开销大。

### Q: volatile 能保证线程安全吗？举例说明

**答**：volatile 只保证可见性和禁止指令重排序，不保证原子性，不能单独保证线程安全。举两个例子：第一，volatile boolean flag; 多线程下 flag = !flag 是不安全的，因为取反不是原子操作。第二，volatile int i; i++ 是不安全的，因为 i++ 分解为 read-modify-write 三步，volatile 只保证 read 和 write 是原子的，modification 步骤可能被其他线程打断。线程安全需要用 AtomicInteger 或 synchronized 解决。

### Q: 什么是伪共享？如何避免？

**答**：CPU 缓存行是读写缓存的最小单位，通常 64 字节。当多个线程操作位于同一缓存行的不同变量时，一个线程修改会导致其他线程的缓存行失效，即使这些线程操作的是完全不同的变量，这就是伪共享。避免方法：使用 @Contended 注解（JDK 8），让 JDK 帮你填充缓存行；或者手动在变量间添加足够长的 padding 数组，使变量落在不同缓存行。

### Q: LongAdder 为什么比 AtomicLong 快？

**答**：高并发下，多个线程同时修改 AtomicLong 时，只有一个线程能成功 CAS，其他线程都自旋重试，造成严重竞争。LongAdder 内部维护了一个 Cell 数组（分段），每个线程映射到不同 Cell 进行 CAS 操作。线程数量通常等于 CPU 核数，分段后每个 Cell 的竞争大大减少。最后需要结果时，将所有 Cell 的值求和。分段越多，并发能力越强，但空间开销也越大。这是典型的空间换时间策略。

---

## 关联笔记

- 关联笔记: [[JVM内存结构]] [[简历知识点总结]] [[运达面试专区]]
- 参考资源: [[MOC-Java面试]]
