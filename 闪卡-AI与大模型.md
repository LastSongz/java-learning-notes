---
title: "闪卡 - AI与大模型"
created: 2026-05-19
updated: 2026-05-19
tags:
  - 类型/闪卡
  - 分类/ai
status: complete
category: ai
---

 #flashcards/AI与大模型

## Transformer 为什么比 RNN 更好？
?
1. **并行计算**：RNN 必须按顺序处理，Transformer 一眼看全部，可并行训练
2. **长距离依赖**：RNN 读到后面前面信息已模糊，Transformer 通过自注意力直接关联任意距离的词
3. **训练速度**：Transformer 可充分利用 GPU 并行能力
> 来源：[[05-Transformer架构]]

---

## 自注意力机制（Self-Attention）的核心思想？
?
每个词去看所有其他词，按相关性融合信息：
1. 每个 Token 生成 Q（查询）、K（键）、V（值）三个向量
2. Q × 所有 K → 相关性分数
3. 分数过 Softmax → 权重（加起来=1）
4. 权重 × 对应 V → 加权求和 → 融合了上下文的新表示
> 来源：[[05-Transformer架构]]

---

## LLM 的训练过程分哪几个阶段？
?
1. **预训练（Pre-training）**：海量无标注文本，学习语言规律（自回归预测下一个 Token）
2. **监督微调（SFT）**：高质量指令数据，学习遵循指令
3. **RLHF（人类反馈强化学习）**：训练奖励模型，用 PPO 优化使输出更符合人类偏好
> 来源：[[06-训练过程-预训练到RLHF]]

---

## Prompt Engineering 的核心技巧有哪些？
?
1. **Few-shot**：给出几个示例，让模型理解任务格式
2. **Chain of Thought（CoT）**：引导模型逐步推理，提升复杂问题准确率
3. **角色设定**：给模型一个明确的角色（如"你是 Java 专家"）
4. **结构化输出**：指定输出格式（JSON/Markdown）
5. **负面约束**：告诉模型不要做什么
> 来源：[[07-Prompt Engineering]]

---

## RAG（检索增强生成）的流程是什么？
?
1. **文档切片**：将知识库文档切成小段
2. **向量化**：用 Embedding 模型将文本转为向量
3. **存储**：存入向量数据库（如 FAISS/Milvus）
4. **检索**：用户提问 → 问题向量化 → 在向量库中检索最相关的片段
5. **生成**：将检索结果作为上下文，交给 LLM 生成回答
> 来源：[[09-RAG检索增强生成]]

---

## LoRA 微调的原理是什么？
?
LoRA（Low-Rank Adaptation）的核心思想：冻结原始大模型权重，只在每层旁路添加低秩矩阵（A×B）来学习增量。
- 原始权重 W 维度：d×d
- LoRA 矩阵：A（d×r）× B（r×d），r 远小于 d
- 训练参数量大幅减少（通常只需原模型的 0.1%~1%）
- 推理时可将 LoRA 权重合并回原模型，无额外推理开销
> 来源：[[12-微调技术LoRA与QLoRA]]

---

## QLoRA 相比 LoRA 做了什么优化？
?
QLoRA 在 LoRA 基础上增加量化优化：
1. **4-bit NormalFloat 量化**：将基座模型量化到 4bit，大幅降低显存
2. **双重量化**：对量化常数也进行量化，进一步节省显存
3. **分页优化器**：利用 CPU 内存缓解 GPU 显存不足
- 效果：在单张 48G 显存 GPU 上可微调 65B 参数模型
> 来源：[[12-微调技术LoRA与QLoRA]]

---

## AI Agent 的核心组成部分？
?
1. **LLM（大脑）**：理解指令、推理决策、生成回复
2. **Memory（记忆）**：短期记忆（对话上下文）+ 长期记忆（向量数据库）
3. **Tools（工具）**：搜索引擎、代码执行器、API 调用等
4. **Planning（规划）**：将复杂任务拆解为可执行的子步骤
> 来源：[[10-AI Agent]]

---

## 什么是 Nacos 的服务发现机制？
?
Nacos 采用心跳机制（UDP）维护实例状态：
- 临时实例（AP）：客户端每 5 秒发心跳，15 秒没收到标为不健康，30 秒没收到则剔除
- 持久实例（CP）：服务端主动探测
- 剔除后通过 Push 机制将最新服务列表推送给订阅者
> 来源：[[面试问题记录]]
