# 13 - 实战微调

> 第四阶段 · 第3课 | 2026-05-06
> 📖 完整课堂讲解版（含逐行注释）：[[13-实战微调-课堂讲解版]]

## 目标：微调一个"中文客服助手"

让模型从通用回答变成温暖的客服语气。

## 第一步：准备训练数据

```jsonl
{"instruction": "你们的产品能退货吗？", "output": "亲爱的客户您好！我们支持7天无理由退货哦~如需办理退货，请在订单页面点击'申请退货'。"}
{"instruction": "发货要多久？", "output": "亲~下单后24小时内发货，2-3天就能收到~"}
```

> 实际需要 500~2000 条，每条都是高质量的 instruction + output 对。

## 第二步：安装依赖

```bash
pip install torch transformers peft trl datasets accelerate
```

## 第三步：完整微调代码

### 加载模型

```python
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer
from peft import LoraConfig, get_peft_model, TaskType

MODEL_NAME = "Qwen/Qwen2.5-1.5B-Instruct"

tokenizer = AutoTokenizer.from_pretrained(MODEL_NAME, trust_remote_code=True)
model = AutoModelForCausalLM.from_pretrained(
    MODEL_NAME,
    torch_dtype=torch.float16,  # 半精度省显存
    device_map="auto",          # 自动分配GPU
)
```

### 配置 LoRA

```python
lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,  # 语言模型任务
    r=8,                           # 秩
    lora_alpha=32,                 # 缩放系数（通常 4×rank）
    lora_dropout=0.1,              # 防过拟合
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],  # 注意力层
)

model = get_peft_model(model, lora_config)
model.print_trainable_parameters()
# trainable params: ~0.08% || 只训练极少参数
```

### 数据预处理

```python
def process_func(example):
    prompt = (
        f"<|im_start|>system\n你是客服助手<|im_end|>\n"
        f"<|im_start|>user\n{example['instruction']}<|im_end|>\n"
        f"<|im_start|>assistant\n"
    )
    full_text = prompt + example["output"] + "<|im_end|>"

    tokenized = tokenizer(full_text, truncation=True, max_length=256)
    prompt_len = len(tokenizer(prompt)["input_ids"])

    labels = tokenized["input_ids"].copy()
    for i in range(prompt_len):
        labels[i] = -100  # prompt部分不学习

    tokenized["labels"] = labels
    return tokenized
```

### 训练

```python
from transformers import TrainingArguments, Trainer, DataCollatorForSeq2Seq

training_args = TrainingArguments(
    output_dir="./output",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,  # 等效batch=16
    learning_rate=2e-4,
    fp16=True,
    logging_steps=10,
    save_steps=100,
)

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset,
    data_collator=DataCollatorForSeq2Seq(tokenizer, padding=True),
)

trainer.train()
model.save_pretrained("./output/lora_weights")
```

### 测试

```python
def chat(question):
    prompt = f"<|im_start|>user\n{question}<|im_end|>\n<|im_start|>assistant\n"
    inputs = tokenizer(prompt, return_tensors="pt").to(model.device)
    outputs = model.generate(**inputs, max_new_tokens=128, temperature=0.7)
    return tokenizer.decode(outputs[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True)

print(chat("你们的产品能退货吗？"))
```

## 训练产出

```
output/lora_weights/
├── adapter_config.json        ← LoRA 配置
├── adapter_model.safetensors  ← LoRA 权重（几MB~几十MB）
└── tokenizer 文件...

→ 只保存 LoRA 小矩阵，不保存整个模型
→ 方便分享和部署
```

## 使用微调后的模型

```python
from peft import PeftModel

base_model = AutoModelForCausalLM.from_pretrained("Qwen/Qwen2.5-1.5B-Instruct")
model = PeftModel.from_pretrained(base_model, "./output/lora_weights")
# → 原始模型 + LoRA = 微调后的效果
```

## 没有 GPU 怎么办？

```
Google Colab → 免费 T4 GPU（15GB 显存）→ 足够微调 1.5B~7B
AutoDL       → 国内 GPU 租赁平台，几毛钱一小时
LLaMA-Factory → 图形界面微调，不用写代码
```

## 关键要点

1. 微调六步 = 加载模型 → 配 LoRA → 处理数据 → 训练参数 → 训练 → 测试
2. 数据 = instruction + output 问答对
3. 预处理关键 = prompt 标签设 -100，只学 response
4. LoRA 权重几 MB → 方便分享部署
5. 使用 = 原始模型 + LoRA 权重
6. 没有 GPU → Colab 免费白嫖

---

**上一课**：[[12-微调技术LoRA与QLoRA]]
**下一课**：[[14-AI编程工具最佳实践]]
