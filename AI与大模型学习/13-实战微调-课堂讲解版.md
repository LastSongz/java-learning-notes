# 13 - 实战微调（课堂讲解版）

> 第四阶段 · 第3课 | 2026-05-06
> 本文件为课堂讲解的完整内容，精简笔记见 [[13-实战微调]]

---

## 目标：微调一个"中文客服助手"

```
需求：让模型用"客服语气"回答问题

微调前：
  用户: "你们的产品能退货吗？"
  模型: "这取决于具体的退换货政策，不同商家有不同规定..."
  → 通用回答，冷冰冰

微调后：
  用户: "你们的产品能退货吗？"
  模型: "亲爱的客户您好！我们支持7天无理由退货哦~
        如需办理退货，请在订单页面点击'申请退货'，
        或联系在线客服帮您处理。祝您购物愉快！✨"
  → 客服语气，温暖热情
```

## 第一步：准备训练数据

```python
# 数据格式：JSONL（每行一条 JSON）
# 文件名：customer_service.jsonl

{"instruction": "你们的产品能退货吗？", "output": "亲爱的客户您好！我们支持7天无理由退货哦~如需办理退货，请在订单页面点击'申请退货'，或联系在线客服帮您处理。祝您购物愉快！"}
{"instruction": "发货要多久？", "output": "亲~下单后我们会在24小时内安排发货哦！一般2-3天就能收到，偏远地区可能需要3-5天。您可以在订单详情里查看物流信息~"}
{"instruction": "你们有优惠券吗？", "output": "亲爱的，我们店目前有满200减30的活动哦！可以在首页领取优惠券。新用户还有首单立减10元的福利，千万别错过啦~"}
{"instruction": "质量有问题怎么办？", "output": "非常抱歉给您带来不好的体验😢！如果收到的商品有质量问题，请拍照联系我们，我们会为您免费换新或全额退款。一定让您满意！"}
{"instruction": "可以开发票吗？", "output": "亲，我们支持开具电子发票哦~下单时在备注里填写发票抬头和税号，发货后会发送到您的邮箱。如需增值税专用发票请联系客服单独处理~"}
```

**实际中需要至少 500~2000 条这样的数据**，这里只展示几条。

## 第二步：安装依赖

```bash
# 核心依赖
pip install torch transformers peft trl datasets accelerate

# 各库的作用：
# torch         → PyTorch 深度学习框架
# transformers  → HuggingFace 模型加载/推理
# peft          → LoRA 实现
# trl           → SFT 训练流程
# datasets      → 数据加载
# accelerate    → 训练加速
```

## 第三步：完整微调代码

```python
"""
用 LoRA 微调一个中文客服助手模型
基于 Qwen2.5-1.5B（小模型，普通电脑也能跑）
"""

import torch
from datasets import Dataset
from transformers import (
    AutoModelForCausalLM,    # 加载语言模型
    AutoTokenizer,           # 加载分词器
    TrainingArguments,       # 训练参数配置
    Trainer,                 # 训练器
    DataCollatorForSeq2Seq,  # 数据整理（自动填充、截断）
)
from peft import LoraConfig, get_peft_model, TaskType  # LoRA 配置


# ==================== 1. 加载模型和分词器 ====================

# 模型名称（HuggingFace 上的开源模型）
MODEL_NAME = "Qwen/Qwen2.5-1.5B-Instruct"

# 加载分词器（把文字变成 Token）
tokenizer = AutoTokenizer.from_pretrained(
    MODEL_NAME,
    trust_remote_code=True,     # 允许加载自定义代码
    padding_side="right"        # 填充方向（右填充）
)

# 加载模型（把模型权重读进显存）
model = AutoModelForCausalLM.from_pretrained(
    MODEL_NAME,
    trust_remote_code=True,
    torch_dtype=torch.float16,  # 用半精度节省显存（每个参数2字节）
    device_map="auto",          # 自动分配到 GPU
)

# 打印模型参数量（确认是 1.5B）
total_params = sum(p.numel() for p in model.parameters())
print(f"模型参数量: {total_params / 1e9:.2f}B")


# ==================== 2. 配置 LoRA ====================

# LoRA 配置
lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,  # 任务类型：因果语言模型（预测下一个词）
    r=8,                           # 秩（rank），控制 LoRA 矩阵大小
    lora_alpha=32,                 # 缩放系数，通常设为 rank 的 4 倍
    lora_dropout=0.1,              # Dropout 防过拟合
    # 指定在哪些层上加 LoRA
    # q_proj, k_proj, v_proj = 注意力层的 Q/K/V
    # o_proj = 注意力层的输出
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],
)

# 把 LoRA 应用到模型上
model = get_peft_model(model, lora_config)

# 打印 LoRA 的可训练参数量
model.print_trainable_parameters()
# 输出类似：trainable params: 1,234,567 || all params: 1,500,000,000 || trainable%: 0.08%
# → 只训练 0.08% 的参数！


# ==================== 3. 加载和预处理数据 ====================

# 准备训练数据（实际中从文件读取）
train_data = [
    {"instruction": "你们的产品能退货吗？", "output": "亲爱的客户您好！我们支持7天无理由退货哦~"},
    {"instruction": "发货要多久？", "output": "亲~下单后24小时内发货，2-3天就能收到~"},
    # ... 实际中这里有 500~2000 条
]

# 转成 HuggingFace Dataset 格式
dataset = Dataset.from_list(train_data)


# 数据预处理函数：把每条数据变成模型能吃的格式
def process_func(example):
    """
    把一条数据变成：
    input:  "<|im_start|>system\n你是客服助手<|im_end|>\n<|im_start|>user\n问题<|im_end|>\n<|im_start|>assistant\n"
    output: "回答"
    """
    # 拼成对话格式（Qwen 的格式）
    prompt = (
        f"<|im_start|>system\n你是一个热情友好的客服助手，用亲切的语气回答问题。<|im_end|>\n"
        f"<|im_start|>user\n{example['instruction']}<|im_end|>\n"
        f"<|im_start|>assistant\n"
    )
    response = example["output"]

    # 把完整对话拼起来
    full_text = prompt + response + "<|im_end|>"

    # 分词（文字 → Token ID）
    tokenized = tokenizer(
        full_text,
        truncation=True,           # 超长截断
        max_length=256,            # 最大 Token 数
        padding=False,             # 不填充（后面统一填充）
    )

    # 标签 = input_ids 的副本
    # 但 prompt 部分的标签设为 -100（模型不学习预测 prompt 部分）
    # 只学习预测 response 部分
    prompt_ids = tokenizer(prompt, truncation=True, max_length=256)["input_ids"]
    labels = tokenized["input_ids"].copy()
    # prompt 部分设为 -100（忽略）
    for i in range(len(prompt_ids)):
        labels[i] = -100

    tokenized["labels"] = labels
    return tokenized


# 应用预处理
tokenized_dataset = dataset.map(process_func)


# ==================== 4. 配置训练参数 ====================

training_args = TrainingArguments(
    output_dir="./output",           # 模型保存路径
    num_train_epochs=3,              # 训练 3 个 epoch（所有数据过3遍）
    per_device_train_batch_size=4,   # 每张卡每次处理 4 条数据
    gradient_accumulation_steps=4,   # 梯度累积 4 步再更新（等效 batch=16）
    learning_rate=2e-4,              # 学习率（LoRA 常用 1e-4 ~ 5e-4）
    logging_steps=10,                # 每 10 步打印一次日志
    save_steps=100,                  # 每 100 步保存一次检查点
    fp16=True,                       # 用半精度训练（省显存）
    optim="adamw_torch",             # 优化器
    warmup_ratio=0.1,                # 前 10% 步数预热学习率
)


# ==================== 5. 开始训练 ====================

trainer = Trainer(
    model=model,                     # 模型（已加 LoRA）
    args=training_args,              # 训练参数
    train_dataset=tokenized_dataset, # 训练数据
    data_collator=DataCollatorForSeq2Seq(
        tokenizer=tokenizer,
        padding=True,                # 自动填充到同一长度
    ),
)

# 🚀 开始训练！
print("开始训练...")
trainer.train()

# 保存 LoRA 权重（只保存小矩阵，不是整个模型）
model.save_pretrained("./output/lora_weights")
tokenizer.save_pretrained("./output/lora_weights")
print("训练完成！LoRA 权重已保存到 ./output/lora_weights")


# ==================== 6. 测试微调后的模型 ====================

def chat(question: str):
    """用微调后的模型回答问题"""
    prompt = (
        f"<|im_start|>system\n你是一个热情友好的客服助手。<|im_end|>\n"
        f"<|im_start|>user\n{question}<|im_end|>\n"
        f"<|im_start|>assistant\n"
    )
    inputs = tokenizer(prompt, return_tensors="pt").to(model.device)

    # 生成回答
    outputs = model.generate(
        **inputs,
        max_new_tokens=128,    # 最多生成 128 个 Token
        temperature=0.7,       # 有一定创意
        do_sample=True,
    )

    # 解码（Token → 文字）
    response = tokenizer.decode(outputs[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True)
    return response


# 测试
print("\n--- 测试微调后的模型 ---")
print(chat("你们的产品能退货吗？"))
print(chat("发货要多久？"))
print(chat("有没有优惠？"))
```

## 训练过程长什么样

```
开始训练...
{'loss': 2.3456, 'learning_rate': 0.0002, 'epoch': 0.1}   ← 第10步
{'loss': 1.8923, 'learning_rate': 0.00019, 'epoch': 0.2}  ← 第20步
{'loss': 1.4567, 'learning_rate': 0.00018, 'epoch': 0.3}  ← 第30步
{'loss': 1.1234, 'learning_rate': 0.00017, 'epoch': 0.4}  ← 第40步
{'loss': 0.8901, 'learning_rate': 0.00016, 'epoch': 0.5}  ← 第50步
...
{'loss': 0.3456, 'learning_rate': 0.00001, 'epoch': 3.0}  ← 训练完成

→ loss 从 2.3 降到 0.3 → 说明模型在学
→ 如果 loss 不降 → 数据或超参有问题
```

## 训练完的文件结构

```
output/lora_weights/
├── adapter_config.json    ← LoRA 配置（rank、目标层等）
├── adapter_model.safetensors  ← LoRA 权重（只有几 MB~几十 MB）
├── tokenizer.json         ← 分词器
├── tokenizer_config.json
└── ...

注意：保存的只是 LoRA 的小矩阵（几 MB）
不是整个模型（几个 GB）
→ 方便分享和部署
```

## 使用微调后的模型

```python
from peft import PeftModel
from transformers import AutoModelForCausalLM, AutoTokenizer

# 1. 加载原始模型（基础模型）
base_model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen2.5-1.5B-Instruct",
    torch_dtype=torch.float16,
    device_map="auto"
)
tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-1.5B-Instruct")

# 2. 加载 LoRA 权重（你训练出来的）
model = PeftModel.from_pretrained(base_model, "./output/lora_weights")

# 3. 就可以用了！
# → 原始模型 + 你的 LoRA = 微调后的模型
# → 推理时自动合并，效果一样
```

## 没有 GPU 怎么办？

```
Google Colab → 免费 T4 GPU（15GB 显存）→ 足够微调 1.5B~7B
AutoDL       → 国内 GPU 租赁平台，几毛钱一小时
LLaMA-Factory → 图形界面微调，不用写代码
```

## 关键要点

```
1. 微调六步：加载模型 → 配置 LoRA → 准备数据 → 设训练参数 → 训练 → 测试
2. 数据格式 = instruction + output（问答对）
3. 预处理关键 = prompt 部分标签设 -100（不学），只学 response 部分
4. LoRA 权重只有几 MB → 方便分享和部署
5. 使用时 = 原始模型 + LoRA 权重 → 效果等同微调后的模型
6. 没有 GPU → 用 Google Colab 免费白嫖 T4 GPU
```

---

**精简笔记**：[[13-实战微调]]
**上一课**：[[12-微调技术LoRA与QLoRA]]
**下一课**：[[14-AI编程工具最佳实践]]
